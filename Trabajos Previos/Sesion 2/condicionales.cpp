
#include <iostream>

using namespace std;

int main(){

    int number = 5;
    /*
    Es posible cambiar el caracter lógico relacional >
    por otros como <, =, !=
    */

    // Evalua si se cumple la condicion de que el numero
    // es mayor a cero
    if (number > 0){ 
        // Code
    }

    // Si el caso anterior falla, se salta a caso
    if else (number == 0){
        // Code
    }

    // Si los demas casos fallaron, ejecuta este
    else {
        // Code

    }

    // Code after if

    // Finish the program
    return 0;
}